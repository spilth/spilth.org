@charset "utf-8";

<%= Rouge::Themes::ThankfulEyes.render(scope: '.highlight') %>

pre.highlight {
  padding: 12px;
}

.project-type {
  position: absolute;
  top: 4px;
  right: 4px;
  color: #02B875;
  border-radius: 2px;
  padding: 2px;
}